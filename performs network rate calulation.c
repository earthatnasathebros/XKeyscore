#include<.datanode.numberwang>
#include<chip.path>
#include<DECRYITPTION CHIP.h>

int main ()

	int maxValue
	int input f1= 001
	int input p100
 function call	[000,001,002]  (array index)  
	--postfix versions teletext player(beta)
	--prefix versoins (-)(+)
 type cast % rate os/os 
print N("time: % os/os \n" hours rates miniutes seconds);
 
